<% include partials/header %>

<h1>This is the Airlines page!</h1>

<a href="/airlines/new">Add New Airline</a>

<% airlines.forEach(function(airline){ %>
    <div>
        <h4><%= airline.name %></h4>
        <img src="<%= airline.image %>">
    </div>
    
<% }); %>

<% include partials/footer %>